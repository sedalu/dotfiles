# Allow-list: Claude owns this directory, so ignore everything
# and opt in only the files this repo maintains.
*

!.gitignore
!CLAUDE.md
!settings.json
!statusline-command.sh

# A parent directory must be re-included before anything inside it can be.
!hooks/
!hooks/*.sh

!skills/
!skills/**
