#!/usr/bin/env bash
#MISE description="Prune stale worktree references"
set -euo pipefail
# shellcheck source=SCRIPTDIR/_repo_root
source "$(dirname "$0")/_repo_root"

git -C "$MAIN" worktree prune --verbose
