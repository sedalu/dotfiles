# Formulae (bash, git, zsh, openssl@3, pkgconf) are managed by mise via
# [bootstrap.packages] in mise/config.toml. This file is casks + Mac App Store.

# macOS only
if OS.mac?
    brew "mas"

    cask "claude"
    cask "tailscale-app"
end
