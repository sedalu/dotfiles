# System formulae (bash, git, zsh, openssl@3, pkgconf) and the mas CLI are
# managed by mise via [bootstrap.packages] (config.toml + config.macos.toml).
# This file is GUI casks only — mise 2026.6.6 can't yet install pkg-type or
# reconcile already-installed app casks, so they stay on Homebrew for now.

# macOS only
if OS.mac?
    cask "claude"
    cask "tailscale-app"
end
