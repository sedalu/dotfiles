#!/usr/bin/env bash
#USAGE complete "Update App Store apps via mas"
#MISE description="Update App Store apps via mas"
#MISE hide=true

set -euo pipefail

[[ "${DOTFILES_OS:-}" != "darwin" ]] && exit 0

# --- update -----------------------------

# No banners of our own — mas streams its own progress when it upgrades something
# and stays quiet when everything is already current.
mas upgrade
