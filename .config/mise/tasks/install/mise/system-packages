#!/usr/bin/env bash
#USAGE complete "Install system packages from [bootstrap.packages]"
#MISE description="Install system packages from [bootstrap.packages]"
#MISE hide=true
set -euo pipefail

# mise's brew manager pours bottles without requiring Homebrew, creating the
# prefix (/opt/homebrew) itself if absent — so this runs independently of the
# brew task. mise-poured and brew-installed kegs coexist in the same prefix.

echo "📦 Installing system packages..."
mise bootstrap packages install --yes
echo "✔ Done."

echo "🎉 System packages installed"
